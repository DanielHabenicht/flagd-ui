/**
 * Constants for JsonLogic standard operators and properties
 * These are defined by the JSON Logic specification
 */

/**
 * The 'in' operator used in JsonLogic evaluator conditions
 * Used to check if a value is within an array
 */
export const JSONLOGIC_IN_OPERATOR = 'in';

/**
 * The 'var' property used in JsonLogic to reference context variables
 */
export const JSONLOGIC_VAR_PROPERTY = 'var';
